algoritmo "conteate10procedimento"
var
contador, a, b, c: inteiro

procedimento contagem (a: inteiro)
inicio
   enquanto (a <= 10) faca
   escreva(a)
      a <- a + 1
      fimenquanto
fimprocedimento


inicio
   a <- 0
   contagem(A)
fimalgoritmo